import "./style.css";
import logo from "./logo.png";

const logoImg = document.querySelector("#logo");
logoImg.src = logo;

const ToDo = () => {
  return {
    title,
    description,
    dueDate,
    priority,
  };
};
